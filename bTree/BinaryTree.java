// binary tree.

// pre search ：preOrder(r) = print r->preOrder(r->left)->preOrder(r->right)
// mid search: inOrder(r) = inOrder(r->left)->print r->inOrder(r->right)
// post search：postOrder(r) = postOrder(r->left)->postOrder(r->right)->print r


class BinaryTree  {


}

// binary search tree;
